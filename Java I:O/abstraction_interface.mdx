# Key difference between abstract and interface 

1. Interface allows only static immutable variables, whereas abstract does not
2. methods inside interface are by conventions static
3. subclass methods are by conventions default when implemented.


