USE EMPDB;

DESCRIBE EMP;

SHOW INDEXES FROM EMP;

CREATE INDEX IENAME ON EMP(ENAME);

SELECT * FROM EMP WHERE ENAME = "Josh";

EXPLAIN SELECT * FROM EMP WHERE ENAME = "Josh";

CREATE INDEX INDEXID ON EMP(EID);



